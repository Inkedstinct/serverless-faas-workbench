apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: linpack-warm
  namespace: knative-functions
spec:
  template:
    spec:
      containers:
        - name: linpack
          image: registry.gitlab.inria.fr/naleblon/knative-functions/functionbench-knative:linpack
